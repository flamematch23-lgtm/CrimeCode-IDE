{
  "name": "@opencode-ai/crimeopus-api",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hosted OpenAI-compatible API server that proxies CrimeOpus models from Ollama with API-key auth, rate limiting, usage logging, and a custom model catalog.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --target=bun --outfile=dist/server.js",
    "compile": "bun build src/index.ts --compile --outfile=dist/crimeopus-api"
  },
  "dependencies": {
    "hono": "^4"
  },
  "devDependencies": {
    "@types/bun": "catalog:"
  },
  "license": "MIT"
}
