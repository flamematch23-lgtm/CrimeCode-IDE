{
  "name": "@opencode-ai/proxy",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.cjs",
  "scripts": {
    "build": "bun build index.js --target=node --format=cjs --outfile=dist/index.cjs"
  }
}
