// Legacy re-export: the implementation lives in @opencode-ai/app so the
// same component powers both the desktop renderer AND the web build.
export { LiveCursors } from "@opencode-ai/app/components/teams/live-cursors"
