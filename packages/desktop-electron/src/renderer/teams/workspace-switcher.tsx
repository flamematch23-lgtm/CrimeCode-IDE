// Re-export — the implementation lives in @opencode-ai/app so the same
// component powers the desktop renderer AND the web build.
export { WorkspaceSwitcher } from "@opencode-ai/app/components/teams/workspace-switcher"
